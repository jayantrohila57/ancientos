/////////--API--LINK--///////////////
const DYNAMIC_URL = "localhost:4000";
////////////////////////////////////
const UPDATES_CHANGE_LOGS = "https://" + `${DYNAMIC_URL}` + "/update";
const DEVICE_BRANDS = "https://" + `${DYNAMIC_URL}` + "/brand";
const TEAM_MEMBERS = "https://" + `${DYNAMIC_URL}` + "/team";
const SCREENSHOTS = "https://" + `${DYNAMIC_URL}` + "/screenshots";
const DONATION = "https://" + `${DYNAMIC_URL}` + "/screenshots";

export default {
	UPDATES_CHANGE_LOGS,
	DEVICE_BRANDS,
	TEAM_MEMBERS,
	SCREENSHOTS,
	DONATION,
};
